import StateTitle from '../../../helpers/state-title';
import { module, test } from 'qunit';

module('Unit | Helper | state title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = StateTitle.create();
  assert.ok(result);
});
