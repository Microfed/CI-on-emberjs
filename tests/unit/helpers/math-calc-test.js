import { mathCalc } from '../../../helpers/math-calc';
import { module, test } from 'qunit';

module('Unit | Helper | math calc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mathCalc(42);
  assert.ok(result);
});
