{{yield}}

{{#if builds.length}}
    {{#bs-accordion selected=selected}}
        {{#each builds as |build index|}}
            {{builds-list-item value=index}}
        {{/each}}
    {{/bs-accordion}}
{{else}}
    <h3>{{t 'builds-list.no-builds'}}</h3>
{{/if}}

