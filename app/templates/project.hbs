<div class="row">
    <div class="col-md-12">
        <div class="page-header">
            <h2>{{model.title}}</h2>

            {{#link-to 'project.builds' model}}Builds{{/link-to}}
        </div>
    </div>
</div>

{{outlet}}
